# 音频文件放这里

把你准备好的完整歌曲文件命名为 **`music.mp3`** 放到本目录（`frontend/assets/audio/music.mp3`）。

支持格式：`mp3` / `m4a` / `wav` / `ogg`。

如果文件是其它格式（比如 `music.m4a`），请同步修改
[frontend/js/content.js](../js/content.js) 里的 `musicFile` 字段。
